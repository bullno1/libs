// The one unit that implements the registrations declared in components.h.
// Here, the dual use macros expand to definitions instead of declarations.
#define BENT_DEFINE_COMPONENTS
#include "components.h"
